# RFID Marathon Management System - Frontend (.env.template)

# Backend API
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Auth (intentionally stored on client as per requirement)
RFID_USERNAME=admin
RFID_PASSWORD=Admin@123456

# Sync
SYNC_RETRY_INTERVAL_MS=5000
SYNC_MAX_RETRIES=5

# UI
APP_NAME=RFID Marathon Manager

# RFID Tag Allotment Defaults (Create Race)
RFID_DEFAULT_PREFIX=2
RFID_DEFAULT_SUFFIX_DIGITS=3
RFID_DEFAULT_SUFFIX_START_NUMBER=130
