services:
  rfid-postgres:
    build:
      context: ./database
      dockerfile: Dockerfile
    env_file:
      - ../backend/.env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
    name: rfid_postgres_data
